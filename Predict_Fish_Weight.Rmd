---
title: "Weight of Fish"
author: "Aetan Barquero"
date: "Fall 2025"  
output: 
  html_document:
    number_sections: true
    toc: true
    toc_depth: 5

 
    
---


```{r include=FALSE,echo=FALSE}
require(tigerstats)
require(tidyverse)
require(car)
require(leaps)
```

# Predicting Fish Weight
```{r}
fish <- read.csv(file = "Fish.csv")
```

```{r eval=TRUE}
 
fish <- fish %>%
mutate(across(where(is.character), as.factor))
levels(fish$Species)
Bream <- filter(fish, Species == "Bream")
Bream <- select(Bream, -Species) 
Parkki <- filter(fish, Species == "Parkki")
Parkki <- select(Parkki, -Species) 
Perch <- filter(fish, Species == "Perch")
Perch <- select(Perch, -Species) 
Pike <- filter(fish, Species == "Pike")
Pike <- select(Pike, -Species) 
Roach <- filter(fish, Species == "Roach")
Roach <- select(Roach, -Species) 
Smelt <- filter(fish, Species == "Smelt")
Smelt <- select(Smelt, -Species) 
Whitefish <- filter(fish, Species == "Whitefish")
Whitefish <- select(Whitefish, -Species) 
```

# For each of the three species of fish listed below, determine the best regression model and also the best 2 element regression model. Use the BestSubset Regression helper file.


## Perch

```{r}
Perch <- filter(fish, Species == "Perch")
Perch <- select(Perch, -Species)
```


### Best Models

```{r}
Perchmodel1 <- lm(Weight~ .,data=Perch)
summary.lm(Perchmodel1)
```



### Best Two regressor model

```{r}
library(leaps)
Perchmodelautomate <-
   regsubsets(Weight ~ .,
               data = Perch,
               nbest = 1,       # 1 best model for each number of predictors
               nvmax = NULL,    # NULL for no limit on number of variables
               force.in = NULL, force.out = NULL,
               method = "exhaustive")

summary.perch <- summary(Perchmodelautomate)
as.data.frame(summary.perch$outmat)
library(car)
subsets(Perchmodelautomate,statistic="adjr2",legend="bottom",main="Adjusted R^2")
```

Using the provided graph above, I found the best single element regression model ,which is height, best 2 element regression model (Height and Width), and I was also able to get the best 3,4 and 5 elements regression models. Statistically, I judged the element regression models by referring to the adjusted R square values. 

## Pike

```{r}
Pike <- filter(fish, Species == "Pike")
Pike <- select(Pike, -Species)
```


### Best Models

```{r}
Pikemodel1 <- lm(Weight~ .,data=Pike)
summary.lm(Pikemodel1)
```

### Best Two regressor model

```{r}
library(leaps)
Pikemodelautomate <-
   regsubsets(Weight ~ .,
               data = Pike,
               nbest = 1,       # 1 best model for each number of predictors
               nvmax = NULL,    # NULL for no limit on number of variables
               force.in = NULL, force.out = NULL,
               method = "exhaustive")

summary.pike <- summary(Pikemodelautomate)
as.data.frame(summary.pike$outmat)
library(car)
subsets(Pikemodelautomate,statistic="adjr2",legend="bottomright",main="Adjusted R^2")
```

By using the provided data in the graph above, I am able to identify the best 2 element regression model is length 1 and length 3. I am also able to define the best element model regression out of all of them, which is length1, length2, length3, height, and width. 

## Roach

```{r}
Roach <- filter(fish, Species == "Roach")
Roach <- select(Roach, -Species) 
```


### Best Models

```{r}
Roachmodel1 <- lm(Weight~ .,data=Roach)
summary.lm(Roachmodel1)
```

### Best Two regressor model

```{r}
library(leaps)
Roachmodelautomate <-
   regsubsets(Weight ~ .,
               data = Roach,
               nbest = 1,       # 1 best model for each number of predictors
               nvmax = NULL,    # NULL for no limit on number of variables
               force.in = NULL, force.out = NULL,
               method = "exhaustive")

summary.roach <- summary(Roachmodelautomate)
as.data.frame(summary.roach$outmat)
library(car)
subsets(Roachmodelautomate,statistic="adjr2",legend="bottomleft",main="Adjusted R^2")
```

By using the provided data regarding "roach", I am able to identify the different element regression models compared. The absolute best element regression model for roach is length 3 and width however, the best one element regression model is width. 

## Final Summary Statements